{
  "name": "CWAD-Scaffolder shared assets",
  "purpose": "Reusable files composed into generated projects by the scaffolding engine.",
  "note": "Shared assets are not copied blindly; the generator resolves only the assets requested by template metadata."
}
