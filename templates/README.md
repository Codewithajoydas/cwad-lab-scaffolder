# CWAD-Scaffolder Templates

Professional, metadata-driven templates for CWAD-Scaffolder.

## Important

This repository is a template source library. The scaffolder should compose shared
assets, render `{{VARIABLE}}` placeholders, copy only the files requested by metadata,
and validate the generated result.

No generated template should contain unresolved variables after rendering.
