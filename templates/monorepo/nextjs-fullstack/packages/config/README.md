# @cwad/config

Reserved workspace for genuinely shared configuration.

Keep application-owned runtime environment validation inside the owning application. Move configuration here only when multiple workspaces actually consume it.
