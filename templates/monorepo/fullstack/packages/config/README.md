# @cwad/config

Shared configuration workspace.

Keep runtime environment parsing inside the application that owns it. Move configuration here only when it is genuinely shared by multiple packages or applications.
